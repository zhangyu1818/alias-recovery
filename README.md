# alias-recovery

code alias recovery.

## Install

```bash
npm i -g alias-recovery
```

## Usage

create a json file in the root directory.

```json
{
  "@api": "src/api",
  "@components": "src/components"
}

```

run the command.

```bash
ar src alias.json
```
